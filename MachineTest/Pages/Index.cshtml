@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container">
    <h2>Home Security Control</h2>
    <p>status: <label asp-for="HomeSecurityStatus">@Model.HomeSecurityStatus</label></p>
    <form>
        <div class="form-group" hidden=@Model.InputCodeVisible>
            <label for="pwd">Code:</label>
            <input type="password" asp-for="HomeSecurityStatus" class="form-control" id="pwd">
        </div>
        <div class="form-group">
            <button type="button" class="btn btn-default" onclick="location.href='@Url.Action("IoTEventAsync", "this")'">Arm</button> 
            <!-- <button type="button" class="btn btn-default" value="@IndexModel.IoTEventAsync()" onclick="document.getElementById('Text1').value = this.value">Arm</button> -->
        </div>
        </form>
    </div>
